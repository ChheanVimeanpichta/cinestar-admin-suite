apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - name: frontend
      port: 80
      targetPort: 80
    - name: backend
      port: 5000
      targetPort: 5000
